import styled from "styled-components"

const StyledTransitionPackText = styled.span`
    color: #000000;
    font-size: 1.95rem;
    font-weight: 500;

    b {
        font-weight: 700;
    }
`

export default StyledTransitionPackText;