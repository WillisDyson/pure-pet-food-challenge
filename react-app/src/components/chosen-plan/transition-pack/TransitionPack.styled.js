import styled from 'styled-components';
const TransitionPackWrap = styled.div`
    align-items: center;
    background-color: #fff;
    border-radius: 20px;
    box-shadow: 0 2px 5px rgba(0 0 0 / 0.2);
    display: flex;
    padding: 12px 22px;
    position: relative;

`;

export default TransitionPackWrap;